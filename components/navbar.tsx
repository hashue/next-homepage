import { Menu, MenuList, MenuItem } from "@chakra-ui/react";

const Nav = () => {
    return (
        <div>
            <Menu>
                <MenuList>
                    <MenuItem>Works</MenuItem>
                </MenuList>
            </Menu>
        </div>
    );
};

export default Nav;
